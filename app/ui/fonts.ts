import { Iceland, Delius } from 'next/font/google';

export const iceland = Iceland({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
});

export const delius = Delius({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
});
